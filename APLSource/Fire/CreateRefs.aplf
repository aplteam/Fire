 CreateRefs
⍝ Create refs if possible at all.
⍝ Tries to find needs stuff in ## first, then in ⎕SE, then it gives up.
⍝ A←GetRefTo'APLTreeUtils2'   ⍝TODO⍝
⍝ F←GetRefTo'FilesAndDirs'    ⍝TODO⍝
⍝ R←GetRefTo'WinReg'          ⍝TODO⍝
 A←APLTreeUtils2
 F←FilesAndDirs
 R←WinReg

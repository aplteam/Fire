 RemoveClones←{
     0=(n.Tab1≡n.IgnoreRefs2.##.##.##.TabObj)⊃G.(IgnoreRefs IgnoreRefs2):⍵
     list←⍵
     (⊂⍕G.LookIn)∊'#' '⎕SE':list
     names←⍕¨list[;0]
     b1←names≡¨⊂G.LookIn
     b2←(0=≢¨names)∨(G.LookIn,'.'){⍺∘≡¨(≢⍺)↑¨⍵}names
     (b1∨b2)⌿list
 }

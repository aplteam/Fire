 MaskText←{
⍝ All text (anything between quotes) in ⍵ becomes ⍺ which defaults to a blank.
     ⍺←' '
     ~(≡⍵)∊0 1:⍺∘∇¨⍵
     string←,⍵
     w←##.Where{⍵∨≠\⍵}''''=string
     string[w]←⍺
     string
 }

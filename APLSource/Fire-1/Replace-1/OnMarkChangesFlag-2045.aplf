 OnMarkChangesFlag←{
⍝ Toggle "Active" of corresponding controls
     n.(MarkChangesType MarkChangesText).Active←(0⊃⍵).State
     n.MarkChangesText.BCol←((0⊃⍵).State)⊃¯1 0
     _←n.MarkChanges.State{(⍺∧⍵):n.MarkChangesType.(Active SelItems)←0(0 1 0) ⋄ ⍬}1<⍴,n.ReplaceBy.Text
     (0<⍴n.MarkChangesText.Text):⍬
     (0=n.MarkChanges.State):⍬
     n.MarkChangesText.Text←'⍝ ',(10↑##.FormatDateTime ⎕TS),' by ',⎕AN,' & Fire'
     ⍬
 }
